[package]
name = "biheap"
version = "0.1.0"
edition = "2021"
description = "A binary heap that supports efficient removal of extreme elements."

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
